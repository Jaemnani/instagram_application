@AGENTS.md

# 키딩성수 — 손대기 전 반드시 확인할 것

이 사이트는 **풀페이지 스냅**(휠 한 번에 한 장면)으로 되어 있다. 아래 규칙을 어기면
스크롤이 흘러가거나 갇혀서, 코드는 멀쩡한데 "화면이 이상한" 상태가 된다.
전부 실제로 한 번씩 겪고 고친 것들이다.

## 1. 장면은 반드시 화면 안에 들어와야 한다

mandatory 스냅은 "모든 장면이 뷰포트보다 작다"를 전제로 한다. 하나라도 넘치면
브라우저가 안착점을 못 잡아 **스크롤이 스르륵 흘러간다**.

- 장면(`.snap-page`)을 추가하거나 내용을 늘렸으면 **낮은 화면(640~750px)에서 높이를 잰다**
  ```js
  [...document.querySelectorAll('.snap-page')]
    .filter(p => p.getBoundingClientRect().height > innerHeight)
  // 결과가 비어 있어야 한다
  ```
- **FAQ가 임계를 정한다.** 문항 수로 높이가 정해져 화면을 따라 줄지 않는 유일한 장면이다.
  문항을 늘리면 `globals.css` 의 `max-height: 900px` 블록에서 간격을 더 줄이거나,
  스냅 임계(`min-height: 640px`)를 올려야 한다.
- 스냅 임계를 **올리는 건 최후의 수단**이다. 760px 로 뒀더니 13" 맥북(뷰포트 ~750px)에서
  스냅이 통째로 꺼져 "휠 돌린 만큼만 움직이는" 평범한 스크롤이 됐다.

## 2. 스냅 장면에 넣으면 안 되는 것

- **`position: sticky`** — 스크롤이 그 지점에 갇힌다(문서 최대 6991인데 5258에서 정지 실측).
- **`overflow: hidden` / `overflow-x: auto`** — 그 자리에 스크롤 포트가 생겨 휠 대상이
  그 요소로 고정된다. 가로 갤러리가 세로 휠을 삼킨 것도, 곰돌이 장면이 흘러간 것도 같은 원인.
  가로 스크롤이 꼭 필요하면 `StripControls.tsx` 처럼 휠을 직접 위임한다.
- **그 구간에서만 휠을 가로채는 연출** — 조리개 셔터를 넣었다가 걷어냈다. 장면마다 휠
  반응이 달라져 쓰기 불편하다. 연출보다 조작 일관성이 우선이다.

## 3. CSS 를 고쳤는데 화면이 그대로면 — 코드보다 서빙 CSS 를 먼저 의심

Turbopack dev 가 `globals.css` 변경을 반영하지 않고 **옛 CSS 를 계속 서빙**한다.
청크 파일명 해시까지 그대로라 눈치채기 어렵다. **이 프로젝트에서 두 번 밟았고, 두 번 다
"고쳤는데 안 된다"의 진짜 원인이었다.** TSX 는 같은 시점에 정상 반영되므로 "CSS만 안 먹는다"가 신호.

```bash
# 파일엔 있는데 서빙 CSS 엔 없으면 확정
for u in $(curl -s localhost:3005/ko | grep -o 'href="/_next/[^"]*\.css[^"]*"' | sed 's/href="//;s/"//'); do
  curl -s "localhost:3005$u" | grep -c "방금-추가한-선언"
done
# 해결: 서버 재시작만으론 부족 — .next 를 지워야 한다
pkill -f "next dev" && rm -rf .next && npm run dev
```

## 4. 검증은 실제 휠로, 처음부터 끝까지, 여러 화면 크기에서

- JS `scrollTo` 점프로만 확인하면 스크롤 갇힘·스냅 문제가 안 드러난다. **실제 휠**로 굴린다.
- **부분 확인 금지** — 장면이 몇 개 안 되니 매번 상단→푸터 완주한다.
- 개발 창 하나로 판단하지 않는다. 창 높이가 넉넉하면 낮은 노트북에서만 나는 문제를 영영 못 본다.
  창 리사이즈가 안 먹는 환경이면 iframe 에 사이트를 띄우고 iframe 크기를 바꿔 **측정**한다
  (미디어쿼리는 iframe 뷰포트 기준. 단 iframe 안으로는 휠이 잘 전달되지 않아 측정 전용).

## 5. 히어로는 건드리지 않는다

LCP 요소이고, `.wordmark-hero` 의 `clamp()` 와 `:lang(ko) keep-all` 은 넘침 구간을 실측해서
맞춘 값이다. 워드마크 문자열이 바뀌면 그 값을 다시 재야 한다(계산 근거는 `globals.css` 주석).

## 6. SEO/GEO 구조는 측정 근거가 있다

- 모든 장식 요소는 `aria-hidden`, 시맨틱 제목은 별도로 둔다(대형 타이포와 `<h2>` 를 겹치지 않게).
- 다크 배경 위 작은 라벨은 **`clay-400`** 만 쓴다(`clay-500` 은 ink-900 위 4.22:1 로 AA 탈락).
- 애니메이션은 JS 미실행·`prefers-reduced-motion` 에서 **내용이 완전히 보이는 상태**가 기본값이어야 한다.
  숨김/흐림 상태는 JS 가 `data-*-ready` 를 붙인 뒤에만 존재하게 만든다.
- 자세한 내용과 이력은 `.claude/MEMORY.md`, 전역 함정은 `~/.claude/TOOL_GOTCHAS.md` 참조.
